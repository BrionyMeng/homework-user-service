CREATE TABLE app_user
(
    id         integer      NOT NULL,
    name       text         NOT NULL,
    age        character(3) NOT NULL,
    created_at date         NOT NULL,
    updated_at date         NOT NULL
);
